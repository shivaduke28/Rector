.rector-right-sidebar {
    width: 360px;
    flex-direction: column;
    margin-top: 40px;
    margin-bottom: 0;
    padding: 20px;
}

.rector-node-detail-name-label-container {
    margin-bottom: 20px;
    align-items: center;
}

.rector-exposed-input {
    flex-direction: row;
    border-width: 1px;
    justify-content: space-between;
}

.rector-exposed-name-label-container {
    flex-direction: row;
    align-items: center;
    width: 100px;
    margin-right: 8px;
}

.rector-exposed-name-label {
    font-size: 14px;
}

.rector-exposed-value-label-container {
    flex-direction: row;
    width: 60px;
    align-items: center;
    justify-content: flex-end;
    margin-left: 8px;
}

.rector-exposed-step-label-container {
    flex-direction: row;
    width: 38px;
    align-items: flex-end;
}

.rector-exposed-step-label {
    font-size: 14px;
    visibility: hidden;
}

.rector-exposed-input--focused {
    border-color: var(--rector-border-color);
}

.rector-exposed-input--focused > .rector-exposed-step-label-container > .rector-exposed-step-label {
    visibility: visible;
}

.rector-exposed-input--focused .rector-label {
    background-color: black;
}

.rector-exposed-value-label {
    font-size: 14px;
}

.rector-exposed-toggle-container {
    flex-direction: row-reverse;
    flex-grow: 1;
}

/* Vector3の見出し。名前だけを出すので、値やスライダーの列は持たない。
   border-widthは行の高さと名前列の位置を .rector-exposed-input と揃えるために要る */
.rector-exposed-vector3-header {
    flex-direction: row;
    border-width: 1px;
    margin-top: 4px;
}

/* 見出しにぶら下がるX/Y/Zの行。名前の列ごと下げるとスライダーの左端がfloat行とずれるので、
   ラベルだけを下げる。RectorCommonの .rector-label { margin: 0 } と同スペシフィシティでは
   負けるため、親クラスを噛ませて上げてある */
.rector-exposed-input--indented > .rector-exposed-name-label-container > .rector-exposed-name-label {
    margin-left: 12px;
}
